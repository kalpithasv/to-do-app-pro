// This file is kept for reference but is no longer used.
// All data is now loaded from localStorage only, starting with empty arrays.
// Users must create their own tasks, projects, and grocery lists.

import { Task, Project, User, GroceryList, GroceryItem } from '@/types';

// Empty arrays - no sample data
export const sampleUsers: User[] = [];
export const sampleTasks: Task[] = [];
export const sampleProjects: Project[] = [];
export const sampleGroceryLists: GroceryList[] = [];
